@rendermode InteractiveAuto
@attribute [StreamRendering]

@if (Vendors is null)
{
    <p>Loading...</p>
}
else
{
    @foreach (var item in Vendors)
    {
        <MudCheckBox @bind-Value="@item.IsSelected" Size="Size.Large" Color="Color.Tertiary">@item.DisplayName</MudCheckBox>
    }
}

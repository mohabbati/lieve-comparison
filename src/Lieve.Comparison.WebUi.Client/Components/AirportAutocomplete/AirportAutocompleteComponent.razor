<MudAutocomplete @bind-Value="Airport"
                 Required
                 Validation="@(new Func<KeyValuePair<string, string>, IEnumerable<string>>(Validate))"
                 T="KeyValuePair<string, string>"
                 ToStringFunc="@(airport => airport.Value)"
                 SearchFuncWithCancel="@SearchFuncWithCancel"
                 Label="@Label"
                 Strict="false"
                 Immediate="false"
                 CoerceText="false"
                 CoerceValue="false"
                 Variant="Variant.Text"
                 ShowProgressIndicator="true">
    <ProgressIndicatorTemplate>
        <MudProgressLinear Size="Size.Small" Indeterminate="true" Color="Color.Default" />
    </ProgressIndicatorTemplate>
    <ItemTemplate Context="airport">
        <MudGrid Spacing="1" Justify="Justify.SpaceBetween">
            <MudItem>
                <MudField Variant="Variant.Text">@airport.Key</MudField>
            </MudItem>
            <MudItem>
                <MudField Label="@airport.Value">@airport.Value</MudField>
            </MudItem>
        </MudGrid>
    </ItemTemplate>
</MudAutocomplete>